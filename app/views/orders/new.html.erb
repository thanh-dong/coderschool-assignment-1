<div class="container">
  <h1>You have chosen <%=params[:food_name].to_s%></h1>
  <div class="col-sm-4">
    <%= render @food, options: {order: false}%>
  </div>
  <div class="col-sm-8">
    <%= render 'form' %>
  </div>
</div>
